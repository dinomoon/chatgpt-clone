export const BASE_URL = '/';

export const AUTH_ROUTES = {
  SIGN_IN: '/signin',
  SIGN_UP: '/signup',
};

export const CHAT_ROUTES = {
  CONVERSATIONS: '/c',
};

export const PUBLIC_ROUTES = [AUTH_ROUTES.SIGN_IN, AUTH_ROUTES.SIGN_UP];
